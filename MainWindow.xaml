<Window x:Class="WPF_Projekt.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_Projekt"
        mc:Ignorable="d"
        Title="AppLauncher" Height="450" Width="800"
        WindowStyle="None"
        ResizeMode="CanResize">

    
    <WindowChrome.WindowChrome>
        <shell:WindowChrome 
            CaptionHeight="0"
            ResizeBorderThickness="5" 
            UseAeroCaptionButtons="False"/> 
    </WindowChrome.WindowChrome>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="35" /> 
            <RowDefinition Height="*" /> 
        </Grid.RowDefinitions>
        
        <!--hier soll die ganze HeadLeiste Definiert werden-->
        <Grid Grid.Row="0" x:Name="HeaderGrid">
            <!--Farb verlauf-->
            <Grid.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1.1,0">
                    <GradientStop Color="LightGray" Offset="0.2"/>
                    <GradientStop Color="DarkGray" Offset="0.7"/>
                    <GradientStop Color="Black" Offset="1.0"/>
                </LinearGradientBrush>
            </Grid.Background>

            <!--Titel-->
            <TextBlock Text="&lt;My App/&gt;" Width="100px" FontSize="15" Foreground="Black" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="5,0,5,0" FontWeight="DemiBold"/>
            
            <!--Menu Items-->
            <Menu Margin="100,0,0,0" HorizontalAlignment="Left" VerticalAlignment="Center" Background="Transparent" BorderThickness="0,0,0,2" BorderBrush="Purple">
                <MenuItem Header="File" Background="Transparent" FontSize="14" Foreground="Black" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="3,2,3,2" BorderThickness="1" BorderBrush="Black">
                    <MenuItem Header="Add" />
                    <MenuItem Header="Remove"/>
                    <MenuItem Header="Settings"/>
                </MenuItem>
                <MenuItem Header="Edit" Background="Transparent" FontSize="14" Foreground="Black" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="3,2,3,2" BorderThickness="1" BorderBrush="Black">
                    <MenuItem Header="Darkmode" x:Name="CheckBox_Darkmode" IsCheckable="True" IsChecked="True" Checked="OnDarkmodeCheckboxChanged" Unchecked="OnDarkmodeCheckboxChanged"/>
                    <!--<CheckBox x:Name="CheckBox_Darkmode" Content="Darkmode" IsChecked="True" Checked="OnDarkmodeCheckboxChanged" Unchecked="OnDarkmodeCheckboxChanged"/>-->
                </MenuItem>
            </Menu>

                    <!--Knoepfe-->
            <StackPanel Orientation="Horizontal"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Right"
                        Margin="0,0,0,0">
                <!-- Minimize -->
                <Button x:Name="MinimizeButton" Style="{StaticResource ButtonTaskBar}" Width="22" Height="22" Content="&#xE921;" FontFamily="Segoe MDL2 Assets" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"
                    Background="LightGray" Foreground="Black" BorderThickness="0" Margin="0,0,5,0"/>

                <!-- Maximize/Restore -->
                <Button x:Name="MaximizeButton" Style="{StaticResource ButtonTaskBar}" Width="22" Height="22" Content="&#xE922;" FontFamily="Segoe MDL2 Assets" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"
                    Background="LightGray" Foreground="Black" BorderThickness="0" Margin="0,0,5,0"/>

                <!-- Close -->
                <Button x:Name="CloseButton" Style="{StaticResource ButtonTaskBar}" Width="22" Height="22" Content="&#xE8BB;" FontFamily="Segoe MDL2 Assets" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"
                    Background="Red" Foreground="Black" BorderThickness="0" Margin="0,0,5,0"/>
            </StackPanel>
        </Grid>
        
        <!--hier ist der Background-->
        <Rectangle Grid.Row="1" 
                   VerticalAlignment="Stretch" 
                   HorizontalAlignment="Stretch">
            <Rectangle.Fill>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1.4,.85">
                    <GradientStop Color="Purple" Offset="-.2"/>
                    <GradientStop Color="LightGray" Offset="0.2"/>
                    <GradientStop x:Name="DarkmodeColor" Color="Black" Offset="0.8"/>
                </LinearGradientBrush>
            </Rectangle.Fill>
        </Rectangle>

        <!--hier ist der Container für alle Apps-->
        <ScrollViewer Grid.Row="1"
                      HorizontalScrollBarVisibility="Disabled"
                      VerticalScrollBarVisibility="Auto"
                      VerticalAlignment="Stretch"
                      HorizontalAlignment="Stretch"
                      Background="Transparent"> 
            <!--hier ist die Scrollbar-->
            <ScrollViewer.Resources>
                <Style TargetType="ScrollBar" BasedOn="{StaticResource ScrollBarRound}">
                    <Setter Property="Margin" Value="0,0,5,0"/>
                    <Setter Property="HorizontalAlignment" Value="Right"/>
                    <Setter Property="VerticalAlignment" Value="Stretch"/>
                </Style>
            </ScrollViewer.Resources>
            <!--hier sind die apps-->
            <WrapPanel Name="AppContainer"
               Orientation="Horizontal"
               Margin="5,10,5,10">
                <WrapPanel.Resources>
                    <Style TargetType="Button" BasedOn="{StaticResource ButtonWrapPanalApp}"/>
                </WrapPanel.Resources>
            </WrapPanel>
        </ScrollViewer>
        
    </Grid>
    
</Window>
