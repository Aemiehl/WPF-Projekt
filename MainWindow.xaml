<Window x:Class="WPF_Projekt.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_Projekt"
        mc:Ignorable="d"
        Title="AppLauncher" Height="450" Width="800"
        WindowStyle="None"
        ResizeMode="CanResize">

    
    <WindowChrome.WindowChrome>
        <shell:WindowChrome 
            CaptionHeight="0"
            ResizeBorderThickness="5" 
            UseAeroCaptionButtons="False"
            /> 
    </WindowChrome.WindowChrome>

    <Grid Name="WholeApp">
        <Grid.RowDefinitions>
            <RowDefinition Height="35" /> 
            <RowDefinition Height="*" /> 
        </Grid.RowDefinitions>
        
        <!--hier soll die ganze HeadLeiste Definiert werden-->
        <Grid Grid.Row="0" x:Name="HeaderGrid">
            <!--Farb verlauf-->
            <Grid.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1.1,0">
                    <GradientStop Color="LightGray" Offset="0.2"/>
                    <GradientStop Color="DarkGray" Offset="0.7"/>
                    <GradientStop Color="Black" Offset="1.0"/>
                </LinearGradientBrush>
            </Grid.Background>
            
            <!--Titel-->
            <TextBlock Text="&lt;My App/&gt;" Width="100px" FontSize="15" Foreground="Black" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="5,0,0,0" FontWeight="DemiBold"/>
            
            <!--Knoepfe-->
            <StackPanel Orientation="Horizontal"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Right"
                        Margin="0,0,5,0">
                <!-- Minimize -->
                <Button x:Name="MinimizeButton" Width="25" Height="25" Content="―"
                    Background="LightGray" Foreground="Black" BorderThickness="0.5" Margin="0,0,5,0"/>
                
                <!-- Maximize/Restore -->
                <Button x:Name="MaximizeButton" Width="25" Height="25" Content="☐"
                    Background="LightGray" Foreground="Black" BorderThickness="0.5" Margin="0,0,5,0"/>

                <!-- Close -->
                <Button x:Name="CloseButton" Width="25" Height="25" Content="✕"
                    Background="Red" Foreground="Black" BorderThickness="0.5" Margin="0,0,5,0"/>
            </StackPanel>
            
           
        </Grid>
        
        <!--hier ist die Scrollbar-->
        <ScrollBar Name="BodyScrollbar" Grid.Row="1" HorizontalAlignment="Right"/>
         
        <!--hier ist der Container für alle Apps-->
        <WrapPanel Name="AppContainer" Grid.Row="1" Margin="10,10,30,10">
            <TextBlock Text="Welcome to the App Launcher!" FontSize="24" Margin="0,0,10,0"
/>
    
        </WrapPanel>


    </Grid>
    
    
    
</Window>
